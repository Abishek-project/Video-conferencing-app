import 'package:flutter/material.dart';

class MeetHistory extends StatelessWidget {
  const MeetHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
